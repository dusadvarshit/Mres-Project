% Function to identify flux pagerank percentile differences

function [v_diff, pr_diff, indices_diff] = flux_pagerank_diff(v2m_a,v2m_b,indices_a,indices_b,percentile_a,percentile_b)
  % This still needs to be worked out  
end
